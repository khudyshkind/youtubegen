# Lefiro — Список задач

> **Актуально по дельту №12 включительно (31.07.2026).**
> Сведено из базового TASKS (17.07), дельт №1–№3, №5–№12, RSS-плана и UX-аудита. Закрытые задачи убраны. Источник каждого пункта помечен: [Δn] — дельта, [база] — TASKS 17.07, [RSS] — RSS-план, [UX] — UX-аудит.
> ⚠️ **Дельта №4 отсутствует** — задачи 20–21.07 могли не попасть в свод.
> ⚠️ **Δ9 переформулировала критичный блок**: часть задач была построена на неверных предпосылках. Опровергнутое помечено ❌ с пояснением, чтобы не восстановилось заново.

---

## 🔴 КРИТИЧНОЕ

- [ ] **Проверить на новых юзерах, лечит ли переделанный дашборд нулевую конверсию.** [Δ9]
  ❌ **Старая формулировка «стартовый баланс не покрывает стоимость видео» опровергнута:** двое из троих «сжигателей» ушли, имея на руках 3 400–4 600 из 10 000 кредитов. Не хватило денег только `romancholak777` (осталось 390), и то после картинок, SEO и превью.
  **Что установлено вместо:** все 7 внешних юзеров пришли **со своим текстом**, ни один не заказал видео по теме, ни один не дошёл до озвучки. До Δ9 дашборд вёл единственной дорогой в 8-шаговую студию (7 кликов до картинок), а инструмент «Иллюстрации» (2 клика) был не виден.
  **Сделано:** дашборд переделан в три блока разделов с перечнем состава.
  **Осталось:** дождаться новых регистраций и посмотреть, куда они идут. Метрика: доля юзеров, попавших в `/tools/*` против `/studio`; доля дошедших до озвучки.
  ✅ **Δ11: появился первый платящий клиент** — `psixozchannel@gmail.com` (`c984d52b-068c-4ea9-bdb7-580f622ba524`). Зарегистрировался 26.07 в 15:35 и **оплатил тариф basic через две минуты** — не пробовал бесплатно, пришёл платить. 27.07 одним заходом сгенерировал ~20 картинок `flux_schnell`, заказал **SEO и превью** — то есть первым из всех дошёл дальше картинок и, судя по порядку шагов, увидел готовое видео. С 27.07 15:55 не возвращался.
  **Выборка в одного человека гипотезу не подтверждает и не опровергает**, но это первое движение метрики за всё время. Что дальше: следить за возвратом (`last_active_at`), за истечением тарифа 25.08 и за тем, повторится ли модель «регистрация → сразу оплата» у следующих.

- [ ] **Гард на обход шагов озвучки/субтитров ИЛИ легализация пути «только картинки».** [Δ9] Кнопка «Далее» блокирует переход (`StepWizard.tsx:309, 319`), но `images/route.ts` наличие аудио/субтитров **не проверяет** — все трое «сжигателей» прошли мимо. **Решение не очевидно:** прямой серверный гард сломает сценарий «нужны только картинки», а именно с ним пришли все семеро. Скорее правильно — не запрещать, а сделать этот путь явным и посчитанным (см. следующий пункт).

- [ ] **Кап 30 сцен в инструменте «Иллюстрации» — разобрать и поднять осмысленно.** [Δ9] Кап стоит в 5 местах (`IllustrationsTool.tsx:499`, `scenes/route.ts:28`, `scene-split.ts:18-23,47`, fallback `:60` — 20), **нигде не обоснован в коде**. Человек со своим аудио на 60–70 минут упирается в 30 картинок.
  **Замеры Δ9 говорят, что поднимать безопасно по FAL** (батч 6–8 картинок = 2.5 с, конкурентность 40), **но стена — фаза Claude**: чанк 27 сцен = 45–53 с, при 400 сценах фаза промптов съест `maxDuration=300`.
  Правильно: привязать кап к реальным пределам и/или дробить фазу промптов, а не менять число на большее.
  ✅ **Δ10: для движка `secretslider` кап обоснован и опущен до 15** (сервер `images/route.ts`, клиент — `max` и клэмп `onChange` зависят от выбранного движка). Причина в комментарии: разброс времени задачи 32–200+ с независимо от размера батча плюс ~3.5 с на заливку каждой картинки; при 30 картинках расчёт даёт 375 с против `maxDuration=300`.
  ✅ **Δ11: кап 15 снят в асинхронном пути** (`images-async/route.ts`, коммит `e02720f`). Обоснование было привязано к `maxDuration` на Vercel — в пайплайне на Railway этой границы нет. Владелец указал, что движок фактически выдавал **54 изображения за раз**, а его собственное ограничение — **не более 60 изображений в минуту**. В синхронном пути `images/route.ts` кап 15 остаётся и обоснован (правило 67). Живая проверка: 29 картинок за 5 мин 20 с ночью, в худшее для движка окно.
  **Для FAL-движков кап 30 в инструменте по-прежнему стоит и по-прежнему ничем не обоснован** — этот пункт остаётся открытым именно для них.

- [ ] **Продолжение прерванной генерации порциями — остаток после асинхронного пайплайна.** [Δ9, Δ10, ✅ основное закрыто в Δ11]
  ✅ **Асинхронный пайплайн построен и в проде** (коммиты `b0582c5`, `e02720f`) — детали в разделе «Закрыто». Хранилищной проблемы больше нет: картинки сохраняются по одной по мере готовности, кредиты списываются поштучно после заливки, задача живёт в `image_jobs` и переживает закрытие вкладки. При обрыве оплаченное остаётся у пользователя.
  **Осталось только UI:** пользователю негде нажать «доделать остальные» — данные на месте, механизма докачки на клиенте нет. Задача оказалась заметно меньше, чем числилась: не хранилище, а кнопка и параметр offset.
  **Осталось также:** отмены запущенной задачи нет вовсе — в `image_jobs` статусы только `pending/processing/completed/failed`, эндпоинта отмены нет. Пока закрыто предупреждающим диалогом (см. ниже), но при больших партиях это ощутимо: 450 картинок Vision Pro = 526 500 кредитов в один клик без права передумать.

- [ ] **Убедиться, что механизм сгорания кредитов реально работает — до 21.08.** [Δ7, переработано Δ10]
  ✅ **Предохранители переделаны и в проде** (коммит `19711ff`, Railway Online).
  **Что было найдено:** единственная проверка `totalPaid > 0 && N/totalPaid > 0.20` при `totalPaid === 0` **пропускала списание вообще без защиты**; сам `totalPaid` брался отдельным запросом, чья успешность не проверялась, а разбор заголовка имел два фолбэка в ноль — сбой запроса был неотличим от честного нуля. Плюс сводка владельцу уходила только при `successCount > 0`, из-за чего молчание в Telegram означало одновременно «истёкших нет» и «крон умер».
  **Что стало:** четыре последовательных предохранителя — недостоверный `totalPaid` → отказ + алерт; противоречие `N > 0` при `totalPaid = 0` → отказ; объём > `EXPIRY_MAX_CREDITS_PER_RUN` (300 000 кр) → отказ; доля > 20% при `totalPaid ≥ 10`, иначе абсолютный кап 5 юзеров. Плюс алерт по каждому случаю с расшифровкой и **ежедневное подтверждение работы крона**.
  ✅ **Δ11: heartbeat подтверждён живьём.** 29.07 в 09:00 UTC пришло «Подписки (cron 09:00 UTC): проверка выполнена, истёкших нет. Платных пользователей: 4» — **первое за всё время подтверждение, что крон вообще запускается** (правило 61 отработало по назначению).

  ❌ **Δ11: дата «первое истечение ~21.08, 452 796 кредитов» ОПРОВЕРГНУТА данными.** Запрос `WHERE plan_expires_at IS NOT NULL` вернул всего две строки, и 21.08 не истекает никто. Запись из Δ7 устарела: тариф владельца с тех пор продлевался (`activatePlan` даёт 30 дней от `max(now, expires_at)`) и уехал на 20.09 с другим объёмом.

  **Фактический календарь истечений (проверено 29.07):**

  | аккаунт | истекает | крон отработает | тарифных кр. | что будет |
  |---|---|---|---|---|
  | `psixozchannel@gmail.com` (клиент) | 25.08 15:37 | **26.08 09:00 UTC** | 68 950 | < 300 000 → пройдёт все предохранители, отработает **целиком и впервые** |
  | `denis-region@mail.ru` (владелец) | 20.09 11:12 | 21.09 09:00 UTC | 703 180 | **> 300 000 → прогон будет ОТКЛОНЁН** с алертом (отказ в безопасную сторону) |

  **Осталось руками владельца:**
  1. **Живой тест `expire_plan` до 26.08.** Механизм не срабатывал в проде ни разу (`operation='plan_expired'` → 0 строк). 26.08 отработает впервые, необратимо, сразу целиком (списание + уведомление юзеру + письмо + сводка) — **и на живом платящем клиенте**. Репетировать на нём нельзя.
     ⚠️ **`yredam4@gmail.com` для теста НЕ ГОДИТСЯ:** `plan='pro'`, но `plan_expires_at = null` и `plan_credits = 0` — все 500 130 в вечном кошельке. Тариф проставлен прямым `UPDATE` мимо `activatePlan`. Если выставить ему прошедшую дату, крон спишет **ноль** и отрапортует успехом — тест подтвердит то, чего не проверял. Нужен аккаунт с ненулевыми тарифными кредитами, начисленными через `add_plan_credits` (не `UPDATE profiles`), и небольшой суммой.
  2. **Порог 300 000 пересмотреть до 21.09** — иначе истечение владельческого аккаунта будет отклонено. Решить: поднять порог, разбить прогон на части или принять отказ как штатный и обработать вручную.
  3. **Проверить Railway logs 27.08** — по факту первого боевого срабатывания.
  ⚠️ Порог 300 000 и кап 5 подобраны под текущий масштаб — пересматривать при росте платной базы.

- [ ] **Юрист — 5 пунктов** (не CC, не автоматизируется). [Δ5, Δ7, Δ8]
  1. «Честная деградация» сценария: в §6 оферты нет основания под приём текста на 80% от цели. Развести «длина — ориентировочный параметр» и «списание по фактическому объёму».
  2. §1.10 называет Баланс «предоплатой», а §5.10 + §6.5 трактуют сгорание как абонентский платёж (ст. 429.4 ГК). Расщепить сущности до ~21.08.
  3. **Трансграничная передача ПДн** — не раскрыта вовсе (Claude/OpenAI/fal/ElevenLabs — US/EU). Ст. 12 152-ФЗ; ст. 18 п.5 — локализация в РФ (Supabase не в РФ). Штрафуется само по себе.
  4. **Основание списания при оверфло объёма** — юзер оплачивает объём, выведенный из раздутого скрипта. «Навязанный объём» по ЗоЗПП жёстче, чем недобор.
  5. Общая вычитка оферты/соглашения/политики. Найдено: §5.9 (3 дня) vs terms §13.2 (7 дней); terms §7.1–7.2 логическая петля; §6.3 удержание комиссий эквайринга с потребителя.
  - Отдельно: юрдокументы ред. 1.0 от 21.07, `legal_acceptances` пишет version 1.0.

---

## 🟡 ЗАПЛАНИРОВАНО (ближайшее)

- [x] **Привязка Telegram через deep link — предложение в момент долгой генерации.** [Δ13] Сейчас `telegram_chat_id` заполняется только у оплативших через бота (`admin/users/activate` строки 148-157), у остальных null — уведомления до них не доходят. Механизм: генерировать одноразовый токен со сроком жизни, показывать кнопку со ссылкой `t.me/<bot>?start=<токен>`, бот по `/start <токен>` находит `user_id` и пишет `chat_id` в профиль. Ник вводить нельзя — Bot API не умеет слать по `@username`, только по `chat_id`.
  ⚠️ Токен обязан быть одноразовым и короткоживущим: иначе чужой Telegram привяжется к чужому аккаунту.
  Место показа — не онбординг, а момент запуска долгой задачи: там ценность очевидна («это займёт несколько минут, подключите бота»). Решение владельца Δ13.
  ✅ Зависимость закрыта: уведомления реализованы в Δ13 (коммит `cherry-pick c4c8e05` на main).
  ✅ **Разведка завершена [2026-08-05]:**
  — `/start` уже умеет парсить deep link payload (`index.js:2103`); при неизвестном токене просто показывает приветствие.
  — Одноразовых токенов в проекте нет; нужна новая таблица `tg_link_tokens` или поле `tg_link_token + tg_link_token_expires_at` в `profiles`.
  — Рекомендован stateful-подход: `randomBytes(16).hex` → таблица с `user_id`, `expires_at = NOW()+15min`, `used_at`.
  — Показывать кнопку в двух местах: `Step5Images.tsx:847` (generating-фаза картинок) и `Step6Video.tsx:802` (queued/processing рендера).
  — Страница настроек `/settings` (`SettingsClient.tsx`) — дополнительное место: там нет ни Telegram-секции, ни кнопки отвязки.
  — В UI нет ни отображения статуса привязки, ни механизма отвязки.

- [x] **Баннер «Подключить Telegram» не отображается во время генерации.** [Δ13] Разведка [2026-08-05] выявила две причины:
  А) Баннер рендерится, но внизу длинного UI (движок + стиль + кнопка + прогресс). Компонент не переключается в «режим генерации» — вся форма остаётся видна. Пользователь не прокрутил.
  Б) `tgLinked` инициализирован `null`, не `false`. Если GET `/api/telegram/link` упал или вернул `ok: false` — `catch(() => {})` молча проглатывает ошибку, `setTgLinked` не вызывается, `null === false` → `false` → баннер не рендерится.
  ✅ **Закрыто [2026-08-05]:** Б — `catch` заменён на `catch((err) => { console.warn(...); setTgLinked(false) })` в Step5Images и Step6Video. А — баннер перемещён внутрь блока прогресса: теперь виден сразу после прогресс-бара, без прокрутки. Остаток (полноценный режим генерации без формы) вынесен в отдельный пункт ниже.
  ✅ **Закрыто [2026-08-05 v2]:** убрано тройное дублирование статуса в Step5 (инфо-блок скрывается при loading, кнопка показывает только «Генерация…», убрана строка счётчика из прогресс-блока). TelegramBanner вынесен за пределы прогресс-блока в обоих файлах и переоформлен: белый заголовок, синяя кнопка #2563EB. Коммит `865d798`.
  ✅ **Закрыто [2026-08-06]:** race condition — уведомление не приходило при привязке посреди генерации. Логи: `[notify] skip: no chat_id` — job завершился до того, как бот записал `telegram_chat_id`. Фикс: в обработчике `/start link_` после привязки — catch-up: проверяем активные задачи (image/audio/video), если нет — ищем завершённые за последние 10 мин и отправляем уведомление. Плюс: кнопка Step5 показывает `step5.btn_loading` = «Генерация…» вместо полного текста фазы. Коммит `fca2530`.
  ✅ **Уточнено [2026-08-06]:** окно catch-up сужено — вместо «последние 10 мин» используется `tg_link_tokens.created_at` (момент нажатия «Подключить»). Задачи, завершившиеся до нажатия, уведомление не получат. Коммит — в этой дельте.

- [ ] **Страховка `injectCharacterProfiles` мертва — починить условие срабатывания.** [Δ12]
  ✅ **Основная задача про консистентность персонажей закрыта в Δ12** (коммит `7776312`, детали в CONTEXT и в разделе «Закрыто»). Причина была не в проводке, а в содержании: `descriptionTask` просил описание «covering species/color/features» — формулировка под животных, для человека дававшая `adult male human, average build, casual appearance`. Переписан, профили получили визуальные якоря, **Haiku теперь копирует их дословно сама** (сцены 2 и 29 одного прогона совпадают слово в слово), владелец подтвердил улучшение глазами.
  ❌ **Осталось:** механическая подстановка кодом на живом прогоне дала `injected=0 prompts_total=29`. Функция ищет в промпте **имя** персонажа, а Haiku пишет туда **описание**: `extracted=2 names=Алексей, Марина` — имена кириллические, промпты английские, строки `Alexey` в них нет. Функция исправна, условие не наступает никогда.
  **Как чинить:** менять не регулярку, а условие — подставлять профиль, если в промпте **нет текста описания**, а не если есть имя. Правка в обоих рантаймах (`route.ts:153`, `index.js:5383`), четыре места вызова. Не срочно: сейчас модель справляется сама, но страховка на то и страховка.
  ⚠️ Смежное: в TASKS есть пункт «живой тест doodle (фикс стиле-зависимого `extractCharacters`)» [Δ1] — подтверждено, что это та же подсистема: `styleConfig.illustrative` переключает `styleDirective` и `descriptionTask` (`route.ts:100–109`, `index.js:5334–5340`), но на факт вызова извлечения не влияет.

- [ ] **Референс в превью копирует содержимое, а не только манеру.** [Δ11]
  **Симптомы владельца:** загружает референс с животными — на выходе появляются посторонние персонажи вместо героев сценария; загружает скрин старого превью с таймером — на выходе таймер в том же углу с рандомными цифрами.
  **Промпт при этом уже написан правильно** — в `generateThumbnailBgWithRef` есть явное различение: перенести `typography treatment` (цвет текста, обводка, форма букв, манера размещения), но отрисовать **новый** заголовок, не слова из референса, и не копировать сюжет и композицию. Плюс в системном промпте Haiku при наличии референса запрещено описывать оформление.
  **Причина архитектурная:** вызывается `fal-ai/nano-banana-2/edit` — это **редактор**, его задача по определению сохранять исходное изображение. Мы текстом просим не копировать сюжет, работая через эндпоинт, чей смысл — сохранять сюжет. **Параметра силы влияния референса у модели нет вовсе** (проверено по схеме fal: ни `strength`, ни `denoise`, ни `image_strength`).
  **Отдельно про таймер:** в режиме `text_mode='overlay'` в промпт уходит `NO TEXT, NO WATERMARKS`, то есть **модель не знает текста превью**, но одновременно получает указание перенести типографику и видит референс с надписью. Противоречивая инструкция.
  **Найденные рычаги (из схемы fal, не догадки):**
  - `system_prompt` — отдельная системная инструкция, уходит в Gemini как system instruction, приоритет выше описания сцены. Сейчас все указания свалены в один `prompt`;
  - `thinking_level: 'high'` — модель разбирает инструкцию с исключениями вместо сопоставления токенов. **Цена: +$0.002 = 60 кредитов (2,5%)**, тир 2400 не двигает;
  - `seed` — не задан вовсе, из-за чего сравнение «до/после» на одном референсе несопоставимо.
  **⚠️ Важно для владельца:** запрет на текст в референсе ставить нельзя — часто нужно, чтобы надпись на превью была в манере референса. Граница проходит внутри типографики: форму берём, содержание нет.
  ❌ **Δ12: замер на фиксированном `seed` невозможен — `seed` у модели не работает.** Проверено тремя вызовами (A без seed, B и C с `seed=12345` и побайтово одинаковым промптом): SHA256 скачанных байтов различны, B и C — 536 486 против 385 249 байт при одинаковом 1376×768. Ошибки валидации нет, поля `seed` в ответе нет. **Замер только тенденцией:** несколько прогонов на одном референсе и оценка глазами, 4–6 генераций = 9 600–14 400 кр.
  ⚠️ **Δ12: воспроизвести симптом пока не удалось** — проектов с плохим превью не сохранилось. **Сначала контрольный прогон** (тема, далёкая от сюжета референса + чужое превью с крупной надписью и заметным элементом в углу, 2400 кр): воспроизводится ли эффект вообще. Если нет — правку не делать, силы на дефолт Vision Studio.
  Порядок: контрольный прогон → замер тенденцией → правка `system_prompt` + `thinking_level`.
  ⚠️ Архитектурная оговорка в силе: `/edit` — эндпоинт редактирования, его смысл в сохранении исходника. Если правка не поможет, следующий шаг — **развести два типа референса по разным эндпоинтам**: композиция через `/edit`, стиль через обычный `nano-banana-2` с текстовым описанием (как в инструменте «Иллюстрации»). Это крупнее: новая развилка в UI и два пути в коде.

- [ ] **Сделать Secret Slider (Vision Studio) дефолтным движком на шаге иллюстраций.** [Δ12, поставлено владельцем]
  **Экономика за:** тир 200 против дефолтного `flux_schnell` 100 — удвоение выручки с картинки при качестве NB-класса; дефолтом идёт 85% всех генераций.
  **Что решить до правки:**
  1. **Однополосность.** Один активный слот на ключ (`concurrent_limit: 1` подтверждён API). Сейчас движок — четвёртая карточка, конкуренция маловероятна; дефолтом он отправит **всех** пользователей в очередь из одного слота, и отказ с ожиданием (`retry_after: 264` наблюдался) станет типичным опытом. Предложение архитектора: не молчаливый откат (запрещён инвариантом), а **явная развилка пользователю** — «движок занят, подождать N с или сгенерировать быстрым за 100?».
  2. **Цена по умолчанию вырастет вдвое** — 29 сцен = 5 800 вместо 2 900. Подтверждение по стоимости (`CONFIRM_GEN_COST_THRESHOLD = 5000`) будет срабатывать почти всегда; порог придётся пересматривать.
  3. Асимметрия путей сохраняется: в студии асинхронный, в инструменте синхронный с капом 15.
  ⚠️ Ватермарка Gemini (счёт 2 из 90) — при дефолте под неё попадут все, а не любопытствующие.

- [ ] **Референс в `/tools/thumbnail-gen` бесплатный, в студии за то же — 30 кр.** [Δ11] В инструменте `/analyze-style` не вызывается вовсе, вместо описания подставляется литерал `'match reference image style, typography, color palette and mood'`; `ref_url` при этом уходит в `nano-banana-2/edit` как изображение. То есть это не «бесплатно то же самое», а **другая, более простая реализация**. Нарушение UI-паритета (правило 12), требует решения владельца: подключить анализ (+30 кр), оставить и зафиксировать комментарием как намеренное (правило 54), или сначала замерить, добавляет ли словесное описание что-то при image-to-image.

- [ ] **`credit_transactions.wallet` — подозреваемая колонка-фикция.** [Δ11] Во всех сорока строках выборки стоит `"mixed"`, включая одиночные списания по −100, которые физически не могли разойтись по двум кошелькам. Похоже, пишется константой. **В метрики не брать, пока не показано, кто и чем её заполняет** (правило 47).

- [ ] **В `image_jobs` нет статуса частичного успеха.** [Δ11] Схема допускает `pending | processing | completed | failed`. При остановке по нехватке кредитов задача помечается `failed` — то есть пользователь получает часть картинок **и красную ошибку одновременно**. Честно, но криво. Решение (нужен DDL): добавить `partial_success` либо отличать по непустому `scene_images` при `failed`.

- [ ] **Осиротевшие файлы в Storage при остановке по кредитам.** [Δ11] Картинка, которая успела залиться, но за которую списание не прошло, остаётся в Supabase Storage: в `paidImages` не попадает (`slice(0, stoppedAt)`), в проект не пишется, пользователь её не видит. Перезапишется при следующей регенерации того же проекта. Мусор небольшой, но учитывать в задаче про уборку медиа.

- [ ] **Заливка картинок идёт по одной — минута накладных на партии в 29 штук.** [Δ11] Наблюдение владельца на боевом прогоне: уведомление от Secret Slider о готовности пришло **на минуту раньше**, чем появились картинки. Это наша заливка: ~3,5 с на штуку × 29 ≈ 100 с. Заливать параллельно (с разумной конкурентностью) — прямая экономия времени ожидания. Смежно с пунктом «уточнить резерв 3500 мс/картинка».

- [ ] **Haiku пишет в промпт указание пропорции.** [Δ12] В боевом прогоне сцена 0 заканчивалась на `9:16 aspect ratio suitable for vertical composition` — при том, что рендер у нас 1280×720, а Secret Slider отдаёт 1376×768. Модель самовольно задаёт вертикальный кадр, и это уходит движку содержательной инструкцией. Чинить класс, а не экземпляр: найти, откуда в системном промпте берётся повод писать про aspect ratio, и запретить.

- [ ] **Дублирование хвоста стиля в промптах.** [Δ12] В промпте подряд идёт `…professional photography., photorealistic, professional photography, sharp detail…` — Haiku дописывает то, что и так добавляет наш `fluxSuffix`. Разбавляет промпт, не ломает. Мелочь.

- [ ] **Шаблонные картинки, не сходящиеся с текстом.** [Δ12, наблюдение владельца] «Несколько изображений подряд из разряда люди поднимают руки вверх и радуются, пастельные тона, красочная картинка» — особенно на текстах до 15 минут с интервалом 10 с (≈90 сцен, два чанка по 50, ~25 слов речи на сцену). Рабочая версия: на коротком и бессодержательном фрагменте Haiku нечего иллюстрировать, и она сваливается в сток-образ успеха — то есть не выполняется правило системного промпта «иллюстрируй то, что в тексте сцены».
  ✅ **Проверяется на уже имеющихся данных, без прогонов:** промпты сохраняются в `scene_images` всех старых проектов. Найти такие сцены и сравнить текст сцены с промптом. Смежная гипотеза: связан ли эффект с позицией сцены в чанке (деградация к концу длинного ответа).

- [ ] **Корневой `Dockerfile` устарел — мина для следующего разворота.** [Δ12] `youtubegen/Dockerfile` в корне репозитория копирует только `video-server/package.json` и `video-server/index.js`, без `image-style-configs.json` и `image-scene-prompts.json`. Именно на нём падал удалённый проект `efficient-rebirth` (`Cannot find module './image-style-configs.json'`). Рабочий — `video-server/Dockerfile`. Корневой сейчас никем не используется, но следующий, кто развернёт проект из корня, получит тот же `MODULE_NOT_FOUND`. Либо удалить, либо привести в соответствие (правило 54: осознанно оставленное фиксируется комментарием).

- [ ] **Состав поставщиков Secret Slider юридически не полон.** [Δ12] Задачи с приложенным файлом обслуживались доменом **`pixel-forge.ru`** (последовательные `gen_1586973`/`gen_1586974` — сквозной счётчик чужой инфраструктуры). Контрольный тест показал, что это балансировка, а не img2img-ветка, но факт второго слоя между нами и моделью остаётся, и в оферте/документации поставщика он не упомянут. **Прямо задевает юрпункт 3 (трансграничная передача ПДн):** в политике перечислены Claude/OpenAI/fal/ElevenLabs, фактических получателей больше. Как минимум — спросить поставщика прямо; как максимум — учесть при вычитке политики.

- [ ] **Логика генерации сцен продублирована в `video-server`.** [Δ11] `video-server/index.js` — CJS без общей сборки, импортировать из `src/lib` не может, поэтому в него портирована вся фаза генерации сцен (вызов Haiku, разбор ответа, сборка промптов).
  ✅ **Частично закрыто:** системные промпты и 11 стилей вынесены в общие JSON (`video-server/image-style-configs.json`, `video-server/image-scene-prompts.json`), которые Next.js импортирует, а video-server читает через `require`; `Dockerfile` копирует оба файла в образ. **Данные не разойдутся.**
  **Осталось:** сам код фазы (функции с префиксом `img*`) существует в двух экземплярах. Правка алгоритма в одном месте не долетит до другого. Вариант на будущее — вынести в общий модуль или собирать video-server бандлером.

- [ ] **Egress на Railway вырастет после переноса заливки.** [Δ11] На 29.07 счёт Railway: **$1,42** за период, из них память $1,04, CPU **$0,0085**, egress $0,36 (7,26 ГБ по $0,05/ГБ). Ожидание ответов от генераторов процессор не грузит — перенос генерации сам по себе почти бесплатен. **Но байты картинок раньше шли FAL → Vercel → Supabase, а теперь для асинхронного пути идут через Railway** (скачать + залить = двойной проход). При росте трафика egress станет главной статьёй. Следить; при необходимости вернуть скачивание-заливку на сторону Vercel, оставив Railway оркестратором.

- [x] ~~**Дешёвый черновой режим превью через разрешение.**~~ [Δ11] ❌ **Δ12: отклонено владельцем.** «Превью — тот элемент, который нужен в самом лучшем качестве». Технически рычаг остаётся (0.5K = 0,75 ставки = 1800 кр вместо 2400, один параметр `resolution`), но снижать качество превью не будем. Записано, чтобы вопрос не всплыл заново.
  **Переформулировка задачи:** удешевлять надо не единицу, а **путь к результату** — `ctac_1971` сжёг 9600 не потому, что превью дорогое, а потому что первые три не подошли. Из трёх рычагов сокращения числа попыток **два опровергнуты фактом в Δ12** (батч вариантов — тарификация per image; `seed` — не работает), остался один: качество референса через `system_prompt` + `thinking_level`.

- [ ] **Панель метрик `/admin/metrics`** — коммит 2 из 2. [Δ7, Δ8, Δ9]
  **Обязательные требования:**
  - исключать аккаунты владельца: `1bc974fa…` = **`denis-region@mail.ru`** (⚠️ Δ11: это **одна запись, а не две** — список был раздут; фильтр по id и по email дают одно и то же исключение), `hudyshkina.e`, `khudyshkin.d3` и тестовый `yredam4`;
  - ⚠️ Δ11: **не исключать `psixozchannel@gmail.com`** — это первый реальный платящий клиент, а не тестовый аккаунт;
  - исключать тестовые операции `video_render_big`, `video_render_test`;
  - **исключать `admin_adjustment`** (Δ9: 6,3 млн кредитов, больше всей остальной эмиссии);
  - помнить, что `credit_transactions` не полностью авторитетен;
  - ⚠️ Δ9: **`projects.credits_spent` всегда 0** — на нём метрику строить нельзя;
  - ⚠️ Δ9: **`projects.duration_minutes` до 27.07 был фикцией для всех ownScript-проектов** (в БД всегда 5). Любая статистика по длительности до этой даты — мусор. Данные «с 27.07» и «до 27.07» не смешивать.

- [ ] **Проверить prompt caching на роутах Opus (сценарии).** [Δ9] На роуте иллюстраций кэш **не работал никогда**: порог для Haiku 4.5 — **4 096 токенов**, а системные промпты ≈1 250 и ≈2 450. Раздувать промпт ради кэша не надо (правило 28), экономия на Haiku мала. **Но Opus дорогой, а порог для Claude Opus 5 — 512 токенов** — там кэш может дать реальные деньги. Проверить структуру запроса и `usage` живого вызова.

- [ ] **Логировать `cache_creation_input_tokens`.** [Δ9] Сейчас печатается только `cache_read` — видна половина картины, `cache_read:0` неотличимо от «кэш пишется, но не читается».

- [ ] **Прогнать `no-undef` по всей кодовой базе.** [Δ9] `tempImageB2Keys` (неработавшая уборка B2) нашёлся случайно при точечной проверке `video-server/index.js`. Что ещё лежит в `src/` — неизвестно. ESLint 9 требует flat config; рабочий рецепт: временный `eslint.config.mjs` с `no-undef` и полным списком globals Node (включая `FormData`, `Blob`, `setImmediate`, иначе 6 ложных срабатываний).

- [ ] **`VF_BASE` — мёртвая константа в video-server.** [Δ10] `index.js:3767` определяет фильтр со `scale=...:force_original_aspect_ratio=decrease` + `pad`, но `getVfFilter` его не использует: рабочий путь — `VF_SCALE = 'scale=1280:720,setsar=1'` (жёсткое приведение без сохранения пропорций). Единственное место, где могли бы появиться чёрные поля. Удалить либо задокументировать как намеренный резерв (правило 54).

- [ ] **Разобрать расхождение баланса `hudyshkina.e@gmail.com`.** [Δ8] ≈486 000 кредитов без записи в `credit_transactions`. Вероятно ручной `UPDATE profiles` мимо RPC. Вывод: журнал транзакций не полностью авторитетен.

- [ ] **Разобрать мусор в `credit_transactions`.** [Δ9] `plan_activation_paddle` на 200 000 при выключенном Paddle; недокументированный `plan_activation_overflow_test`; поле `operation` принимает произвольный текст (в базе есть `Тест`, `Просто так`). Как минимум — валидация допустимых значений `operation`.

- [ ] **Заполнять `projects.credits_spent`.** [Δ9] Сейчас всегда 0. Нужен для счётчика «Потрачено на проект» из редизайна студии и для метрик.

- [ ] **`topic` содержит JSON конфига вместо темы.** [Δ9] 7 проектов у `vitalikchingiz` и `denisregion.ultra`: `{"engine":"flux_schnell","style_value":...}`. Найти путь, который так пишет.

- [ ] **Проверить `phase_updated_at` на ближайшем реальном рендере.** [Δ8] Единственная метрика 010, не подтверждённая живьём.

- [ ] **Взглянуть на expand-rate ~06.08** [Δ7] — копится в `analytics_events` (`script_expand`).

- [ ] **Референс-изображение — итерация 2 инструмента «Иллюстрации».** [Δ5, база] Открытый вопрос: принимает ли fal-движок image-to-image / style-reference.

- [ ] **Подтвердить `[len=2]` у `FAL_CONCURRENCY_LIMIT`.** [Δ9] Переменная пересоздана вручную после обнаружения BOM (`[len=3]`), контрольный замер в логах не сделан. Не критично — код срезает BOM через `env()` независимо. Заодно просмотреть остальные переменные, заводившиеся вставкой из PowerShell.

---

## ⚙️ ИНФРАСТРУКТУРА И НАДЁЖНОСТЬ

- [ ] **Аудит GRANT'ов по всем таблицам разом.** [база, Δ8] У `service_role` отсутствовал INSERT на `analytics_events`; у `anon`/`authenticated` висел лишний TRUNCATE. По функциям аудит сделан. Прогнать SELECT/INSERT/UPDATE/DELETE по всем public-таблицам.
- [ ] **Мониторинг аномального расхода кредитов.** [база, Δ8] Алерт на: юзер сжёг >X за час, операция в цикле, скачок расхода.
- [ ] **Уборка медиа выключена — корень заполнения Supabase.** [база] `cleanupExpiredMedia` (cron 0 4 * * *) в DRY_RUN. Перед `RETENTION_DRY_RUN=false` — запустить вручную в dry-run, посмотреть лог. Не включать вслепую.
- [ ] **B2 — слепое пятно retention:** cleanup ищет префикс `users/`, аудио в `audio/...`, прокси-картинки в `temp/...`. [база] ✅ Δ9: **одна из причин найдена и устранена** — удаление temp-картинок после рендера не выполнялось из-за ошибки скоупа (`tempImageB2Keys` объявлялась в `try`, проверялась в `finally` через `typeof`). Теперь работает. Проверить, сколько мусора накопилось за всё время и убрать разово.
- [ ] **Auto-recharge VGF** — владелец отложил. [Δ7] Написать в VGF (please_help@verygoodffmpeg.com) — попросить эндпоинт баланса.
- [ ] **Sentry-факт: доставленное событие в Issues не предъявлено** (нужен временный тест-роут). DSN video-server на Railway не проверен. [база]
- [ ] **Watchdog для `generating_images` — частично закрыт.** [база] ✅ Δ11: вотчдог расширен на `image_jobs` (`WATCHDOG_IMAGES_TIMEOUT_MIN`, дефолт 15 мин): зависшая в `pending/processing` задача помечается `failed`, проект сбрасывается из `generating_images`, вызывается идемпотентный возврат. Плюс `recoverOrphanedImageJobs()` при старте контейнера — подхватывает задачи, брошенные рестартом Railway. **Осталось:** синхронный путь `images/route.ts` под вотчдог по-прежнему не попадает — там статус проекта ставится, а записи задачи нет.
- [ ] **Recovery pending audio_jobs при старте воркера.** [база]
- [ ] **Панель балансов следит за деньгами, но не за лимитами.** [база] Пороги: место в Supabase, суточный egress B2, квота YouTube API. (Правило 32.)
- [ ] **Логи Vercel живут 1 час** (Hobby) — диагностика вслепую. [база] Δ9 добавляет: **поиск в дашборде логов не ищет по тексту сообщения**, отсутствие строк при фильтре ничего не доказывает. Ещё один аргумент за живой Sentry.
- [ ] **Мониторинг балансов остальных провайдеров** (по образцу fal.ai): OpenAI, SecretVoicer/Voicer, ElevenLabs, **Secret Slider**. [база] Δ9: баланс Anthropic кончился посреди работы — все сцены ушли в fallback, юзер платил бы за мусор. Алерт сработал постфактум; нужен предупредительный порог.
  ✅ **Δ12: у Secret Slider `GET /api/v2/balance` проверен живьём** — отдаёт `api_credits` (782 на начало разведки, 772 после), плюс `total_requests`, `requests_today`, `rate_limit_per_minute: 60`. Самый простой из всех для подключения, заголовок `X-API-Key`.
  🔴 **Δ12: мониторинг apihost МЁРТВ и молчит об этом.** В логах Railway каждые 30 минут `[apihost/balance] API unavailable, skipping`. Движок реально используется (27 операций за 90 дней), проверка не работает, алерта нет — `skipping` в логе единственный признак. Правило 61: молчание мониторинга двусмысленно. Разобрать первым, это не новая функция, а починка существующей.
  🔴 **Δ12: баланс fal — примерно на неделю.** Из панели: `Current Credit Balance` **$13.39**, `Daily Burn` **$1.80**, расход за период $51.55. Проверить, что порог алерта (`fal_balance_alert_state` в коде) настроен и срабатывает.
- [ ] **OpenAI billing-ошибки не отслеживаются** notifyBillingError (только Anthropic). [база]
- [ ] **Выпрямить связывание платёжного тега.** [Δ8] Поиск строки по `MAX(created_at)` за 10 секунд вместо id.
- [ ] **Тег для `plan_activation_paddle`.** [Δ8]
- [x] ~~**Миграция `video_jobs.warnings jsonb` — проверить, выполнена ли.**~~ [база 22.07] Выполнена: `ALTER TABLE public.video_jobs ADD COLUMN IF NOT EXISTS warnings jsonb;` есть в `supabase/schema.sql`, закоммичена в `7c223cb` (Δ2, Phase 5 retry + honest degradation).
- [ ] **«Взаимный сторож»: Vercel-крон пингует Railway `/health` → алерт в Telegram.** [база 22.07] Задумывался как бесплатная замена платному тарифу UptimeRobot с уведомлениями в ТГ. ⚠️ Возможно перекрыт heartbeat из Δ11 — сначала проверить, что именно сейчас сторожит Railway, и только потом решать, нужен ли отдельный пинг.

## 🗄️ ХРАНИЛИЩЕ (экономика)

- [ ] **Картинки на B2/R2** (486 МБ в Supabase). [база] Сначала включить уборку.
- [ ] **Миграция легаси-аудио:** 46 файлов / 275 МБ в Supabase, 23 живых проекта ссылаются. [база]
- [ ] **Экономика хранения перевёрнута:** дорогое хранилище (Supabase) держит картинки+аудио, дешёвое (B2/R2) — видео. [база]
- [ ] **Ограничение длины видео по тарифу.** [база] Free — до N минут.

## 🎬 РЕНДЕР И ГЕНЕРАЦИЯ (качество, не блокеры)

- [ ] **Chunked-путь сценариев (≥30 мин)** — отдельный фикс промпта. [Δ6]
- [ ] **Контроль длины скрипта: LLM-оверфло ×11** (запрошено 5 мин/650 слов → 7600 слов → 58 мин аудио). [Δ2] ⚠️ Δ9 уточняет: **у ownScript оверфло не LLM-ный, а пользовательский** — юзер вставляет сколько хочет текста, капа на длину ввода НЕТ ни на клиенте, ни на сервере. Обе разновидности лечатся по-разному, не путать. **Связано с юрпунктом 4.**
- [ ] **Phase B перекодирует crf 28→20 и раздувает файл вдвое.** [база] Минимум: crf 20→28 в Phase B.
- [ ] **Живой тест doodle** (фикс стиле-зависимого extractCharacters). [Δ1]
- [ ] **Озвучка 56 мин** — тест таймаутов 275→600с + heartbeat (97a8ee0). [Δ1, база]
- [ ] **Кап картинок в студии — 450**, обоснован комментарием в коде (70 мин ÷ 10 с = 420). [база, Δ9] Прежняя оценка «безопасный предел ~280 (7 батчей FAL × 40)» замерами Δ9 не подтверждается: FAL идёт батчами по 40 последовательно и быстро. Реальный ограничитель — фаза Claude и `maxDuration=300`.
- [ ] **ШАБЛОН/ЧЕКЛИСТ нового стиля иллюстраций:** каждый стиль обязан иметь в `claudeInstruction` явную директиву «опиши subject/action/setting». [база]
- [ ] **Источник языка own_script:** у ownScript-проектов `projects.language=null`, store дефолт 'ru'. [база]
- [ ] Усилить цветовые эффекты видео. GPU-кодирование (написать в VGF). 4K. [база]
- [ ] **Прогресс внутри `concat/xfade` — UI висит на 60% десятки минут.** [база 22.07] На живом рендере heartbeat видит работу, пользователь — нет: полоса стоит, признаков жизни ноль. Добавить промежуточные вехи прогресса в UI. Смежно с общей проблемой ожидания без обратной связи.
- [ ] **Живой тест сценария (б) фазы-зомби: пропуск шага 5 → Phase 5 skipped.** [база 22.07] В старом файле помечен «так и не прогнан владельцем». Сценарий (в) прогонялся, (б) — нет.

## 🖼️ ДВИЖКИ КАРТИНОК (резерв и экономика)

- [ ] **Gemini (NB2 Lite) — расконсервация после запуска.** [база] Всё готово: аккаунт khudyshkin.d3@gmail.com, ключ у владельца, осталось Buy credits ($25) + ключ в env + промпт интеграции + алерт на prepay. Цели: −14% себестоимости NB + вендорный резерв.
- [ ] **Пересмотр маржи картинок ×3 → ×2.5–2** как ценовой рычаг. [база] ⚠️ Δ10: **фактическая формула в коде — не «×3 от закупки», а `кредиты = себестоимость в $ × 30 000`** (сходится на `nano_banana`, `gpt_mini`, `thumbnail`; `flux_schnell` округлён вверх с 90 до 100). Наценка ×3 живёт в рублёвой цене кредита, а не в `CREDIT_COSTS`. Пересматривать нужно то, что действительно есть в коде (правило 47).

- [ ] **Однополосность Secret Slider — один активный слот на API-ключ.** [Δ10] Пока пользователей мало — невидимо; при первом реальном трафике станет главной проблемой движка: второй пользователь, нажавший «сгенерировать» во время чужой генерации, получает отказ с ожиданием (наблюдалось `retry_after: 264`). Сейчас закрыто сторожем `GET /tasks/active` + внятным сообщением, но это объяснение отказа, а не его устранение. Варианты: пул ключей с балансировкой, либо общая очередь. Пул тянет за собой учёт баланса по каждому ключу и, возможно, конфликтует с их условиями — уточнить у поставщика.

- [ ] **Уточнить резерв 3500 мс/картинка по фактическим логам.** [Δ10] Число в расчёте бюджета — оценка, а не замер, помечено комментарием в коде. После накопления статистики взять медиану и худший случай из строк `[images/secretslider] scene N upload: Xms` и заменить. При 15 картинках завышенный резерв съедает 52 с бюджета ожидания.

- [ ] **Поштучный `UPDATE projects` в secretslider-ветке.** [Δ10] Сохранение в БД идёт внутри цикла по сценам — на 15 картинках это 15 подряд идущих апдейтов, тогда как в батчевой ветке сохранение одно на батч из 8. Смысла в поштучном нет: все URL получены до цикла, вылет по времени после этого невозможен. Не блокер, лишняя нагрузка на Supabase.
- [ ] **Резервный агрегатор Replicate.** [база]
- [ ] **Кандидаты в палитру** (на fal, по спросу): Seedream, Ideogram/Recraft, NB2 как «топ»-тир. [база]
- [ ] **Инвентаризация id движков gpt vs gpt_mini**, ElevenLabs переценить/скрыть. [база]
- [ ] **Переименование движков в UI (имя = тир качества).** [база] Решение принято, ждёт нейминга. Менять только display-имена, id не трогать.
- [ ] **Развилка движка doodle — решение владельца.** [база 22.07] Расщепление стиля (люди то стик-фигуры, то объёмные; звери местами реалистичные) на `flux_schnell` промптом не лечится: у движка нет `negative_prompt`. Варианты: оставить schnell с честной подписью «менее предсказуемый стиль» ИЛИ сделать дефолтом doodle `flux/dev` с пересчётом цены. ⚠️ Смежно с пунктом «живой тест doodle» — тест покажет, насколько эффект жив сейчас.

## 🤖 TELEGRAM-БОТ

- [ ] **Автоматизация приёма USDT-оплаты ботом.** [база] Блокер массовых продаж. Cryptomus — кандидат.
- [ ] **AI-консультант готов, но канал невидим** — с 17.06 ноль обращений. [Δ7] Проверить почту support@lefiro.co и логи Resend. Если писали туда — консультанта в первую очередь на сайт. Δ9 усиливает: семь юзеров ушли молча.
- [ ] **Тикеты insert-only:** статусы answered/closed в схеме есть, кода перехода нет. [Δ7]
- [x] **Ссылка на пост в теме группы ведёт на лендинг.** [Δ13] ✅ f4b41b9 — `groupPostLink` читает `tg_group_username` из `bot_settings`; при наличии username строит `t.me/{username}/{threadId}/{msgId}`. Без username — `t.me/c/{numId}/...` для приватных групп, либо `null` (ссылка не вставляется). `channelPostLink` вынесен из хардкода в `tg_channel_username`. Промпт генерации поста: запрет `#`, `##`, `---`, списков.
- [ ] **Детектор релизов не видит Railway.** [Δ13] `checkVercelDeploy` (index.js:3524) опрашивает только Vercel API раз в 30 минут. Деплои video-server на Railway бот не замечает вовсе — а именно там живут рендер, генерация картинок и уведомления. Обновления этой части в «Обновления» не попадут.
- [ ] **Кнопка «Оплатить» в основное меню бота.** [база 22.07] ❌ Исходная жалоба («первое сообщение — Укажи какой тариф») опровергнута: `video-server/index.js:1955-1967` уже шлёт нормальное приветствие + PUBLIC_KB с 4 кнопками (Тарифы, Как это работает, FAQ, Поддержка). Осталось: оплата уходит отдельным вторым сообщением с inline-кнопками (`index.js:1969-1973`), а не пунктом единого меню. Решить: добавлять пятую кнопку «Оплатить» в PUBLIC_KB или оставить как есть.

## 📊 АНАЛИТИКА (RSS-заходы 2–6, не начаты)

- [ ] **Ложный тренд** (говорим растущему каналу, что он падает). [RSS, база] Фикс: «просмотры в день» + исключать ролики моложе 7 дней.
- [ ] **Правки отчёта «Канал» (замечания владельца а–д).** [база]
- [ ] **Заход 2 — keywords 2020→303 юнита.** [RSS]
- [ ] **Заход 3 — «Пульс канала»: бесплатный отчёт, 0 юнитов.** [RSS] Меняет воронку.
- [ ] **Заход 4 — переключатель «лонг/шортс/всё».** [RSS]
- [ ] **Заход 5 — velocity-мониторинг.** [RSS] Моат.
- [ ] **Заход 6 — замыкание на продукт** (кнопка «Создать видео» из любого отчёта). [RSS]
- [ ] **Открытые вопросы владельцу по RSS:** снимать ли BYOK с канальных отчётов; «Пульс канала» бесплатно или за минимум; keywords до топ-3 или 10 ключей; сколько каналов на юзера в velocity. [RSS]
- [ ] **Deep-linking аналитики `?tab=`.** [база]

## 🎨 UX И СТУДИЯ

> ❌ Δ9: пункт «иллюстрации уходят батчем БЕЗ предупреждения о стоимости» **опровергнут** — цена показывается до нажатия (`Step5Images.tsx:634–642`): «−N кр./шт · X илл. · Итого: Y кр.», плюс в сайдбаре. Юзеры видели «54 илл. · 5400 кр» и всё равно нажимали. Лечится не показом цены.

- [ ] **Гард на батч от остатка баланса.** [Δ8, переформулировано Δ9] Цена видна, но не видно, **что она означает**: юзер не понимает, что 54 картинки — следствие длины его текста, и что дальше ещё озвучка и рендер. Идея: предупреждать, если операция съедает больше трети баланса, с расшифровкой «N × 100 = X кр, останется Y». Порог «>20k кр» из UX-аудита для free-юзера недостижим.
- [ ] **Цена превью 2400 при итеративном использовании.** [Δ8] `ctac_1971` сжёг 9600 из 10 000 четырьмя превью. Δ9 подтверждает цену данными: `thumbnail` — 121 операция, максимум 2400, всего 225 478 кр. Варианты: дешёвый черновой рендер, первая попытка бесплатно, батч вариантов за одну цену.
- [ ] **Редизайн студии — продолжение** (каркас готов). [база] Шаг 1: иерархия (Тема+Язык+Длительность крупно, остальное в аккордеон); умный дефолт языка = язык интерфейса (опасно: EN-юзер молча генерит RU); запоминание настроек прошлого проекта; **счётчик «Потрачено на проект»** (⚠️ Δ9: `credits_spent` всегда 0, сначала починить поле); снос старых нижних кнопок.
- [ ] **UX Заходы В/Г** (из аудита). [UX, база] В — сводки решений на шагах; Г — мини-превью субтитров на шаге 7, staleness-баннеры, крупная итоговая цена у кнопки шага 4.
- [ ] **Авто-режим студии (3.2)** — разведка готова. [база]
- [ ] Кнопки выбора стиля разного размера. Session-only баннер шага 5. Авто-пересчёт таймкодов картинок из новых субтитров. [база]
- [ ] **`/settings` недоступна из бокового меню** — только прямой URL. [Δ9]
- [ ] **Баг отображения обложки в `/tools/thumbnail`.** [база 22.07] ⚠️ UI-код чист: `<img>` в `tools/thumbnail-gen/page.tsx:355` корректен, рендерится под `{thumbUrl && ...}`, `thumbUrl` ставится из `p.thumbnail_url` (стр.79) и `json.data.thumbnail_url` (стр.157). Разведка сузилась: проверить `src/app/api/generate/thumbnail/route.ts` — сохраняет ли эндпоинт `thumbnail_url` в БД и возвращает ли в `data.thumbnail_url`.
- [ ] **Слабые «зомби»: `image_style` и `voice_id` предвыбраны из прошлой сессии.** [база 22.07] Значения видны до платного клика, пользователь может принять их за свой выбор. Низкий приоритет, но родня уже вылеченным фазам-зомби.
- [ ] **Step5Images не переключается в режим генерации.** [Δ13] Во время генерации вся форма (выбор интервала, движка, стилей) остаётся видна. Баннер и прогресс уходят вниз, за экран. Нужен режим «идёт генерация» — скрыть форму, показать только прогресс + баннер, как это сделано в IllustrationsTool.

## 💡 ИДЕИ РОСТА (архитектор, на рассмотрение)

- [ ] **⭐ Сделать «свой текст + своё аудио» первоклассным путём.** [Δ9] **Самый сильный сигнал сессии:** все 7 внешних юзеров пришли со своим текстом, внешних проектов «по теме» ноль. Идея: явная развилка на входе — «сгенерировать всё» / «у меня есть текст» / «у меня есть текст и аудио». Третий путь: загрузил аудио → Whisper → сцены по **реальным** тайм-кодам → картинки → рендер. Длительность честная (из аудио, не из `слов ÷ 130`), цена считается точно. Технически всё уже есть: студия принимает свой .mp3/.wav на шаге 4.
- [ ] **«Первое видео бесплатно»** (демо-прогон, возможно с вотермаркой). [база]
- [ ] **«Серии» / профиль канала.** [база] Сохранять профиль (стиль+голос+интро+формат) → однотипные видео в 1 клик.
- [ ] **Ретеншн-петля через уведомления:** «тренд по нише обновился». [база] ✅ **Δ13: «видео/картинки/аудио готово» реализовано** — `notifyUserJobDone` в video-server, порог 90 с, три уровня логов. Осталась петля по тренду ниши.
- [ ] **Общий кэш аналитики между юзерами** по ключу (ниша+рынок+язык). [база]
- [ ] **«Перевод сценария» — отдельный инструмент.** ⏸️ Отложено. [база]
- [ ] **Премиум-апскейл превью** через Magnific/Freepik. [база]
- [ ] **Счётчик себестоимости входа** («сколько стоит N-мин видео») на лендинг/шаг 1. [база]

## 📄 ЛЕНДИНГ И ДЕМО

- [ ] **Landing v2 (редизайн) — накоплен в working tree, не закоммичен.** [2026-08-06]
  19 untracked файлов составляют полноценный редизайн: `design-system/lefiro/MASTER.md` (токены/палитра), `public/showcase/` (8 PNG-витрин, ~6.4 МБ), `src/components/landing-v2/` (18 компонентов: Hero, Pricing, FAQ, Bento, ShowcaseReel…), `src/lib/i18n-v2.ts`, `src/app/preview-landing/page.tsx`, `src/components/dev/ThemePreview.tsx` (временный, удалить после выбора темы A/B/C).
  Незакоммиченные M-файлы в рабочем дереве: `package.json`/`package-lock.json` (+gsap ^3.15.0) и `src/app/globals.css` (новая токен-система + временные .theme-a/.theme-b/.theme-c).
  **Ветка `redesign-landing` есть локально, в origin НЕ запушена.** Рекомендация: переключиться на ветку и закоммитить весь блок одним PR.
  Showcase-изображения (`gen_1622905`…`gen_1622957`) ссылаются из `landing-v2/data.ts` — без них preview-маршрут не откроется.
- [ ] **Демо-видео для лендинга** (SHOW_DEMO_SECTION=false). [база] Витрина результата. Δ9: **никто из внешних юзеров не увидел готового видео** — ни одного рендера. Витрина закрывает часть проблемы до регистрации.
- [ ] Отзывы и счётчики выдуманные — заменить на реальные. OG-карточка. Счётчики хиро в SSR = нули. 7→8 шагов. Обрезанный заголовок. [база]

## 🏢 БИЗНЕС (владелец)

- [ ] **Юрлицо** (блокер Paddle/карт). [база] Кандидаты: сербское предузетник/DOO, ИП Армения/Казахстан/Грузия, US LLC. Запуск не блокирует — USDT работает. Архитектор может собрать сравнительную таблицу.
- [ ] **Биллинг-профиль Google на чужом имени** (Ivan Strekozkin) — при юрлице переехать. [база]
- [ ] **Инвентарь платных подписок инфраструктуры** — ревизия раз в месяц. [база] Railway Hobby $5, Cloudflare ~$30/год, Vercel Hobby $0, Supabase Free, Resend Free 100/день.
- [ ] **Предупредительный порог на баланс Anthropic.** [Δ9] Кредиты кончились посреди рабочей сессии; алерт пришёл только по факту ошибки. Нужен порог «осталось меньше X».

## 🔒 БЕЗОПАСНОСТЬ И ТЕХДОЛГ

- [ ] **Ротация засветившихся ключей** (пред-запусковый чек-лист): SECRETVOICER_API_KEY и др. [база] ✅ Δ10: ключ Secret Slider, попавший в чат вместе с их документацией (их доки подставляют ключ в примеры), отозван и перевыпущен; новый заведён в Vercel через панель, в чат не попадал.
- [ ] **Проверить, достижимы ли из UI операции `video_render_big` / `video_render_test`.** [Δ8]
- [ ] **Убрать мёртвый дубль `spend_credits`** либо задокументировать. [Δ8]
- [ ] **Pre-push hook `node --check`** в репо → scripts/ + core.hooksPath. [Δ3] ⚠️ Δ9: **`node --check` недостаточен** — он не поймал `ReferenceError: ownerId is not defined`, уронивший прод. В хук добавить `no-undef`.
- [ ] **`control_job.tmp` и `scripts/*.mjs` в untracked** — разобрать, нужное закоммитить, мусор в `.gitignore`. [Δ8, Δ9] ✅ Δ10: частично закрыто — `scripts/ss-*` (тестовые картинки, JSON, probe-скрипт) и `control_job.tmp` внесены в `.gitignore`. Остаются `scripts/acceptance-language-fix.mjs`, `check-vault.mjs`, `test-byok-live.mjs`, `test-keywords-real-miss.mjs`, `test-quota-balance.mjs`, `test-titles-niche.mjs`. ⚠️ Δ11: добавился `eslint.undef-check.mjs` (временный flat-config для прогона `no-undef` по `video-server/index.js`) — он **полезен и воспроизводим**, разумнее закоммитить его в `scripts/` как рабочий инструмент, чем пересоздавать каждый раз (в Δ11 он уже терялся между заходами).
  ✅ **Разведка завершена [2026-08-06]:** полный инвентарь 19 untracked путей + 3 M-файла — см. handoff.md этой сессии и таблицу ниже.
  **Решение по каждому пути (требует действий владельца):**
  ✅ **Закрыто [2026-08-06]:**
  - Удалены зонды: `check-vault.mjs`, `fal-seed-probe.mjs`, `ss-ref-probe.mjs`, `eslint.undef-check.mjs` (корень)
  - Закоммичены тесты (`341ac3b`): acceptance-language-fix, test-byok-live, test-inject-characters, test-keywords-real-miss, test-quota-balance, test-titles-niche, eslint.undef-check (scripts/), fal-nb2-edit-probe, test-prompt-truncation.js
  - landing-v2 закоммичен на `redesign-landing` (`2428254`), ветка запушена в origin
- [ ] **Заменить сентинель 'Свой текст' на колонку `projects.own_script boolean`.** [база] Δ9: сентинель уже используется как признак в разборах данных — при локализации студии инфер молча умрёт.
- [ ] Два ffmpeg в Docker video-server. `TOOL_SLUG_REMAP` пластырь. `searchParams`-промис на tool-страницах. Глюк «—» в шапке баланса. [Δ5, Δ6, база]
- [ ] Подавление алертов на тестовых job'ах. Race condition на лендинге (`getSession` ~100мс). Следить за тиром VGF. [Δ5, Δ6]

---

## 📌 ЗАКРЫТО В ПОСЛЕДНИХ СЕССИЯХ (краткая память, детали в git и дельтах)

- **Консистентность персонажей — причина найдена и вылечена** [Δ12], коммит `7776312`. Разведка показала, что механизм цел: `extractCharacters` (`route.ts:94`) / `imgExtractCharacters` (`index.js:5333`) работают в обоих рантаймах, `charSection` доезжает до user-сообщения, ownScript-ветки нет вовсе. **Настоящая причина найдена по фактическим промптам из `scene_images`** (без единого нового прогона): профиль был пуст по содержанию — `Alexey, adult male human, travel content creator, average build, casual appearance` — потому что `descriptionTask` просил описание «covering **species**/color/features», формулировку под животных. Плюс verbatim-копирование не выполнялось: профиль подставлялся один раз, дальше шло голое имя (`fresh determined Alexey`). Переписан `descriptionTask` в обоих рантаймах и обеих ветках `illustrative`: 15–35 слов, обязательные визуальные якоря, явный запрет `average build` / `casual appearance` / `adult male human` / `ordinary` / `typical`. Добавлена подстановка кодом + лог `[characters]`. **Живой прогон: профили содержательные, Haiku копирует дословно сама** (сцены 2 и 29 совпадают слово в слово), владелец подтвердил глазами.
- **Таймаут асинхронного пути Secret Slider переработан** [Δ12], коммит `9cbb359`. Инцидент: задача `53362` упала по внутреннему таймауту `budget=466s`, **отработав у поставщика полностью** — деньги уплачены, результат выброшен. Причина — правило 67 второй раз на том же движке: бюджет времени существовал из-за `maxDuration=300` на Vercel и механически переехал на Railway, где ножа платформы нет. Снят пре-флайт-бюджет в async (в синхронном пути остаётся и обоснован), введён `IMAGES_ASYNC_POLL_MAX_MIN` (дефолт 30 мин), **добавлен финальный запрос статуса перед пометкой `failed`**, `WATCHDOG_IMAGES_TIMEOUT_MIN` поднят 15→45 (иначе вотчдог убивал бы живую задачу), `partial_success` убран из условий успеха, текст ошибки сделан честным, удалены мёртвые `ssBudgetMs`/`uploadReserveMs`/`requestStartMs` вместе с константой `600_000`. Добавлены замеры `task start` / `task done` / `task NOT done`. **Живая приёмка: задача `53388`, 32 промпта, `duration=126s`, `polls=22`** (дневное окно).
- **Колонка `image_jobs.provider_task_id`** [Δ12]: id задачи поставщика, пишется сразу после создания. До этого задача, брошенная рестартом или вотчдогом, была невосстановима — картинки есть, деньги уплачены, ручки нет. Подтверждено живьём.
- **Заброшенный проект Railway удалён** [Δ12]: `efficient-rebirth` (сервис `youtubegen`), создан 12.06 на 22 минуты раньше боевого, подписан на тот же репозиторий, **собирался на каждый push полтора месяца и падал** на устаревшем корневом `Dockerfile`. Пользовательских переменных ноль — потому и был безобиден. Боевой сервис после удаления проверен фактом: `deployment ID 67a6635f` не изменился, `/health` → 200 `{"ok":true}`, `[startup/*]` в логах нет, все ключи на месте.
- **Три гипотезы по удешевлению превью опровергнуты фактом** [Δ12] — записано, чтобы не всплыли заново:
  1. **Secret Slider как движок превью** — не годится: нет `system_prompt`, `negative_prompt`, силы влияния референса; `num_images` тарифицируется поштучно; **референсное изображение не поддерживается** (файл принимается multipart-парсером под любым из семи имён и игнорируется сервером — доказано контрольным тестом с чужеродным референсом и побайтово одинаковым промптом).
  2. **Батч вариантов дешевле** — нет: тарификация **per image**, подтверждено биллингом fal (`nano-banana-2/edit`: 41 Images × $0.08 = $3.28).
  3. **`seed` для воспроизводимости** — принимается, но игнорируется: три вызова, SHA256 байтов различны, B и C 536 486 против 385 249 байт при одинаковом разрешении.
- **Возможности API Secret Slider выяснены живыми запросами** [Δ12]: `balance`, `tasks/active` (`concurrent_limit: 1`), `styles`, **`POST /api/v2/user-styles` (пользовательские стили текстом — новое)**, `docs` → Swagger + `openapi.json`. Детали в CONTEXT. Заодно найден второй бэкенд `pixel-forge.ru`.
- **Асинхронный пайплайн генерации иллюстраций — в проде** [Δ11]: коммиты `b0582c5` (Railway + БД) и `e02720f` (студия). Таблица `image_jobs` (20 колонок, по образцу `video_jobs`/`audio_jobs`), эндпоинты `POST /generate-images` и `GET /image-status/:jobId` на video-server, прокси `/api/generate/images-async` и `.../status` на Vercel, поллинг раз в 5 с в `Step5Images`. Запуск через `setImmediate`, восстановление брошенных задач при старте контейнера, вотчдог 15 мин, идемпотентный возврат через `credits_refunded_at IS NULL`. **Списание поштучное, после заливки картинки в Storage** — при обрыве оплачено ровно то, что получено. **Живая приёмка: 29 картинок за 5 мин 20 с ночью**, в худшее для движка окно; синхронным путём это невозможно (потолок 300 с).
- **Secret Slider в студии — «🎨 Vision Studio»** [Δ11]: четвёртая карточка движка, сетка 2×2. Кап 15 снят (см. критичный блок). Добавлено **подтверждение по стоимости**: `CONFIRM_GEN_COST_THRESHOLD = 5000` кредитов в `types.ts`, диалог для **всех** движков, текст «до N кредитов» (списание поштучное) плюс предупреждение, что остановить генерацию после запуска нельзя. Проверено живьём: 29 илл. × 200 = 5800 кр → диалог сработал.
- **Единый источник промптов и стилей для двух рантаймов** [Δ11]: `video-server/image-style-configs.json` и `video-server/image-scene-prompts.json` — Next.js импортирует, video-server читает через `require`, `Dockerfile` копирует оба в образ. Из `images/route.ts` и `image-style-configs.ts` вынуто ~290 строк констант. Синхронный путь проверен живьём после выноса: акварель отрабатывает.
- **Референс стиля на шаге 5 студии был фикцией — исправлено** [Δ11], коммит `f7b6bb2`. Описание от `/analyze-style` клалось в `imageStyle` и уходило полем `image_style`; `getStyleConfig` не находило free-form строку среди 11 ключей и возвращало `DEFAULT_STYLE_CONFIG`. **Пользователь платил 30 кредитов, получал дефолтный фотореализм и вдобавок терял выбранный пресет** — референс делал результат хуже, чем его отсутствие. Теперь описание уходит отдельным полем `custom_style` (как в инструменте «Иллюстрации»), пресет не затирается, приоритет референса виден в UI пометкой. Тот же путь починен в перегенерации одной картинки (`/api/generate/image-single` — отдельный роут, в разведке всплыл попутно). Добавлен `console.warn`, когда `image_style` не совпал ни с одним ключом. **Живая приёмка: LEGO-референс → картинки в LEGO-стиле.**
- **Цены вынуты из i18n во всех местах** [Δ11], коммиты `82c64a4`, `d2ba2a5`. 22 ключа × 2 языка. **Главная находка попутно: пять ценников аналитики врали в сотни и тысячи раз** — на кнопке «−8 кр.» при списании 12 000 (`channel_plan`), «−6 кр.» при 7000 (`niche_finder`), «−4 кр.» при 1800, «−3 кр.» при 1500, «−6 кр.» при 2000. Аналитика открыта только платным, то есть под ударом был ровно тот, кто заплатил; до аналитики никто из платных дойти не успел. Причина та же, что у референса: цены поднялись с июньской шкалы, i18n остался. **Подписи референса** (−60 на шаге 5, −2 в превью, +2 в инструменте) приведены к фактическим 30. **`faq.a4` больше не обещает потолок 780**: диапазон вычисляется из движков, **доступных в UI** (100–1170), `gpt_mini` исключён с комментарием — первая версия правки ошибочно подставила его 1230.
- **Крон подписок считал платными не тех** [Δ11], коммиты `81c7098`, `eb58849`. Признак был `plan != 'free'` → в heartbeat уходило «Платных пользователей: 4», из которых три — владельческие и тестовый. Стало `plan != 'free' AND plan_expires_at > now()` → 2. Это же число — вход предохранителя `totalPaid`, то есть защита калибровалась по завышенной базе. Добавлена строка «Тарифы мимо механизма: N» — считает профили с тарифом и `plan_expires_at IS NULL` (сейчас 2: `hudyshkina.e`, `yredam4`), то есть записи, сделанные прямым `UPDATE` мимо `activatePlan`, **которые не истекут никогда**. Первая версия счётчика считала аномалии вычитанием и в день истечения клиента посчитала бы его самого аномалией — заменено прямым запросом.
- **Новый движок иллюстраций Secret Slider — в проде** [Δ10]: третий тир между дешёвым и дорогим, 200 кр/шт, доступен в инструменте «Иллюстрации» (в студии сознательно нет). Качество NB-класса подтверждено живым тестом: персонаж держится через 6 независимых генераций, дословное описание из промпта переносится. Проверено на бою: акварель и мультяшный стиль отрабатывают, списание ровно по факту (1000 кр за 5 картинок; за упавшие попытки — ноль). Адаптер асинхронный, один POST на весь батч. Коммит `ba82608` + фикс-коммит со сторожем конкурентности, динамическим бюджетом времени и капом 15.
- **Рассинхрон цен в инструменте «Иллюстрации» устранён** [Δ10]: поле `credits` в `ENGINE_OPTIONS` было мёртвым, цена показывалась текстом внутри i18n-строк и синхронизировалась руками. Теперь подпись собирается как метка + `opt.credits` из `CREDIT_COSTS` + отдельный ключ единиц (`tools.ill_engine_unit`). Рассинхронизировать цену в UI со списанием больше нельзя.
- **Предохранители сгорания кредитов переделаны** [Δ10]: см. критичный блок выше. Коммит `19711ff`.
- **Маппинг стилей Secret Slider не нужен** [Δ10] — зафиксировано, чтобы вопрос не всплыл заново. Наши 11 стилей это текстовые `fluxSuffix`, которые подмешиваются в `styledPrompt` **до** вызова адаптера, то есть уже доезжают до движка. Их поле `style` (107 значений) мы намеренно не передаём: два указания стиля подрались бы. Источник стиля один — наш `fluxSuffix`.
- **Навигация и входы** [Δ9]: дашборд переделан из одной кнопки «Создать видео» в три блока разделов (Студия 8 шагов / Инструменты 9 / Аналитика 10 из 11) с перечнем состава и кликабельными пунктами. Заведён `src/lib/content-config.ts` — единый источник `TOOL_CARDS`, `ANALYTICS_GROUPS`, `STUDIO_STEPS`; `/tools`, `/analytics` и дашборд читают один массив. `Studio` добавлена в `NAV_ITEMS`. Число шагов в тексте берётся из длины массива. Коммиты `4c19fee`, `3fb0463`.
- **Честная длительность ownScript** [Δ9]: `duration_minutes` теперь пишется в БД (PATCH `/api/projects/[id]`, debounce 800 мс, валидация 1–120, ownership, явная проверка `error`). Раньше колонка была фикцией — всегда 5. На шаге 2 показывается «Примерная длительность ~N мин». Коммиты `37c4d7a`, `8a35ccb`.
- **Цены шагов** [Δ9]: добавлены строки цены на озвучке и субтитрах, где их не было вовсе. Озвучка считает по той же функции, что сервер — реагирует на смену движка И голоса (проверено живьём).
- **Замеры генерации** [Δ9]: логирование `claude_phase`, `batch N done`, `SUMMARY`, `concurrency_env`. Установлено: FAL быстр (батч 6–8 картинок ≈ 2.5 с при конкурентности 40), стена — фаза Claude (чанк 27 сцен ≈ 45–53 с). Коммит `1c358ab`.
- **Санитизация env от BOM** [Δ9]: `env()` применён ко всем чтениям `process.env`, влияющим на поведение, во фронте, роутах и video-server. Подтверждён BOM в `FAL_CONCURRENCY_LIMIT` (`[len=3]`), переменная пересоздана. Коммит `82f7d55` + два фикса последствий.
- **Два бага в video-server** [Δ9]: `ownerId is not defined` (прод лежал ~10 мин, найден по Deploy Logs) и `tempImageB2Keys` — **уборка temp-картинок из B2 не работала никогда**, маскировалась проверкой `typeof x !== 'undefined'` в другом скоупе. Найден прогоном ESLint `no-undef`.
- **Метрики и безопасность RPC** [Δ8]: миграция 010 патчила мёртвую `spend_credits`; `analytics_events` не писались НИКОГДА (нет `GRANT INSERT`); **закрыта дыра** — все 7 денежных RPC имели дефолтный `EXECUTE` для PUBLIC. Миграция 011 + фикс `schema.sql` + `analytics.ts` проверяет `error`.
- **AI-консультант в боте** [Δ7]: Haiku, книга знаний в system, лимит 10/час, эскалация `[ESCALATE]`. `safeSendMessage`. Снят потолок накопления кредитов (миграция 003). Сбор метрик (миграция 010).
- **Боевые платежи** [Δ6]: ЮKassa ShopID 1413901, СБП 2,2%, фискальный чек. SMTP через Resend. Юнит-экономика (маржа ≈63%). 1080p откачен.
- **Платежи и волны** [Δ5]: два кошелька, сгорание тарифных, `activatePlan`. Юрдокументы ред. 1.0.
- **Подписочная модель** [Δ3]: миграции 001–003, expire_plan, гейт аналитики.
- **libass/субтитры** [Δ2]: Retry Phase 5 + heartbeat + честная деградация.
- **Инфраструктура** [Δ1]: R2 для финальных видео, startup-recovery + watchdog + SIGTERM + heartbeat, Watch Paths.
- **Уведомления в Telegram о завершении долгих задач** [Δ13]: `notifyUserJobDone(userId, kind, payload)` в video-server, три точки вызова (render/images/audio), порог 90 с, best-effort (никогда не бросает), три уровня логов (`log` — нет chat_id или бот заблокирован; `warn` — сбой sbGet или неожиданная Telegram-ошибка). Coммиты: cherry-pick `c4c8e05` на main. ⚠️ Уведомления доходят только до оплативших через бота — у остальных `telegram_chat_id = null`; привязка через deep link в 🟡 ЗАПЛАНИРОВАНО.
- **CLAUDE.md — конфликт инструкций устранён** [Δ13]: «Ритуал завершения» расширен до 6 пунктов, handoff.md сделан обязательным с явной строкой. «Файлы состояния» оставлен как справочник без дублирующих инструкций. Коммиты `c4c8e05`, `d6c82e6` (main).
- **Глушение бота в супергруппе** [Δ13]: guard в начале webhook-обработчика — `chatType === 'group'|'supergroup'` → return, если не reply-to-bot и не @-команда. Попутно: `msgThreadId` прокидывается в fallback-ответы, если бот всё же отвечает в группе. Закрыто коммитом.
- **Публикация в темы супергруппы** [Δ13]: `publishToChannel(text, imageUrl, target, threadId)` — добавлен третий/четвёртый параметры, поддержка `'channel'|'group'|'both'`. `groupConfig` (groupId/threadUpdates/threadNews) загружается из `bot_settings`. Три клавиатуры (preview/monitor/deploy) заменены с одной кнопки «Опубликовать» на три: «В канал / В группу / Везде». Добавлены `tmeNumericId()` и `groupPostLink()` для корректных ссылок на темы супергруппы (`t.me/c/{id}/{threadId}/{msgId}`). Отсутствие `tg_group_id` не роняет бота — выводит понятную ошибку.
- **task-protocol.md — три правки по итогам первого handoff** [Δ13]: (1) п.3 части 4 усилен — кандидат дописывается в файл WORKFLOW.md, не только в handoff.md; (2) «Что не получилось» в шаблоне — если всё получилось, прочерк; (3) кандидат про «два раздела / агент выполняет первый» перенесён из handoff.md в WORKFLOW.md раздел «Кандидаты в правила». Коммит: см. текущий.
- **Привязка Telegram через deep link** [Δ13], коммит `9460822`. Таблица `tg_link_tokens` (миграция 013). API: `POST /api/telegram/link` (токен randomBytes(16), TTL 60 мин, инвалидирует предыдущие) / `GET` (статус) / `DELETE` (отвязка). Бот: ветка `link_<token>` в `/start` — три отдельных ответа (недействительна / использована / устарела), запись `telegram_chat_id`. Секция в `SettingsClient`. Баннер в `Step5Images` + `Step6Video` только для непривязанных. **Требует действий владельца: 1) запустить миграцию 013 в Supabase SQL Editor; 2) добавить `TELEGRAM_BOT_USERNAME` в env Vercel и Railway.**
- **groupPostLink/channelPostLink вынесены из хардкода** [Δ13], коммит `f4b41b9`. `groupPostLink` читает `tg_group_username` из `bot_settings`: при наличии username → `t.me/{username}/{threadId}/{msgId}`; при только `tg_group_id` → `t.me/c/{numId}/...`; ни того ни другого → `null`. `channelPostLink` аналогично читает `tg_channel_username` — строка `lefiro_channel` из кода убрана. Промпт генерации поста: добавлен явный запрет `#`, `##`, `---`, списков через `- / *` (Telegram их не рендерит, читатель видит сырые символы).
- **Атомарность claim `bot_settings` — подтверждена существующей схемой** [база 22.07]. ✅ `key text primary key` в `supabase/schema.sql:576` — это и есть уникальный индекс. `src/lib/telegram.ts:97` на него опирается, паттерн UPDATE-then-INSERT, дубль отбивает БД. Формулировка задачи («сейчас SELECT→INSERT») была неверна.
