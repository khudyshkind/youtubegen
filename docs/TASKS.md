# TASKS — Задачи Lefiro (актуально: 22.07.2026)

> Источник правды по приоритетам, открытым задачам и бэклогу.
> Обновлять после каждой сессии.

---

## ТОЧКА ВХОДА НОВОГО ЧАТА

**Блокер №1 — ЮKassa на модерации** (заявка 134832066). Пока не одобрят боевой магазин, live-ключей нет.  
После одобрения → **ПАКЕТ ЗАПУСКА** (CONTEXT §3.5) одним заходом.

**Приоритеты после пакета запуска:**
1. **Референс-изображение** — итерация 2 инструмента «Иллюстрации» (заказ владельца). Открытый вопрос: принимает ли наш fal-движок image-to-image / style-reference или нужен другой эндпоинт и другая цена.
2. Хвосты бэклога (см. ниже).

---

## ОТКРЫТЫЕ ЗАДАЧИ

### Инструменты

- [ ] **Баг отображения обложки** (`/tools/thumbnail`): картинка генерируется, но на странице сломанный `<img>` + текст. Разведка: thumbnail_url в БД записан? src на странице какой? «Сохранено ✓» честная? — это последний гвоздь волны 1.
- [ ] **Другие tool-страницы**: проверить клиентский restore по `?run=` (ловушка `searchParams`-промиса Next.js 16 — работает только серверный props-паттерн; субтитры и иллюстрации уже правильные).

### Рендер и видео

- [ ] **Живой тест libass-фикса**: короткий проект → явно включить тумблер субтитров → рендер → приёмка: субтитры в видео ЕСТЬ, янтарного баннера НЕТ. Одновременно — первый живой прогон сценария (в) фазы-зомби. Также проверить, выполнена ли SQL-миграция `ALTER TABLE video_jobs ADD COLUMN warnings jsonb`.
- [ ] **Прогресс внутри concat/xfade**: UI висит на 60% десятки минут при живом рендере (heartbeat видит, юзер нет). Добавить промежуточные вехи прогресса в UI.
- [ ] **Контроль длины скрипта**: LLM-оверфло ×11 (запрошено 5 мин / 650 слов → 7600 слов / 58 мин). Разведка: где задаётся таргет слов, есть ли кап, что показывается юзеру перед платными шагами.

### Analytics

- [ ] **Отчёт «Канал» — правки а–д**: две формулы вовлечённости, жаргон «бакет/ø», разница тегов, блок расписания, кнопка «Снять видео» → «План канала».

### Doodle

- [ ] **Разведка doodle-нарушений** (промпт готов): 6 сцен живого теста (таймкоды: 00:51.44 «?», 02:21.16 «???», 02:50.64 балуны с текстом, 03:42.80 сердечки-символы, 04:00.20 закорючка, 05:18.96 мысленный пузырь). По каждой: промпт провоцировал (санитайзер пропустил какое слово?) или движок сам. В тот же отчёт: (а) `maxRetries` конструкторов Anthropic в `images/route.ts` — если дефолт SDK (2 ретрая), тайм-бюджет чанков сцен взорван изнутри → нужен `maxRetries:0`; (б) CREDIT_COSTS + себестоимость fal: 1 картинка schnell vs flux/dev — для развилки движка.
- [ ] **Развилка движка doodle** (вердикт владельца после разведки): расщепление стиля (люди то стик, то объёмные; звери местами реалистичные) на schnell промптом не лечится (нет `negative_prompt`). Варианты: schnell + подпись «менее предсказуемый стиль» ИЛИ дефолт doodle на flux/dev.

### ТГ-бот

- [ ] **Бот-витрина**: /start = меню (О сервисе / Тарифы / Задать вопрос / Оплатить / Сообщить о проблеме) вместо текущего «Укажи какой тариф хочешь оплатить».
- [ ] **База знаний** (`knowledge/lefiro_kb.md`, коммит ba33aea, никуда не подключён): обновить под финальную реальность — тарифы 30 дней, два кошелька (тарифные сгорают / докупленные вечны), аналитика только на платных, retention 72ч (обещание), «кредиты сохраняются при продлении», free: 10 000 разово + без аналитики, ВСЕ инструменты волны 1+2+3; цены только из `/api/plans`-источника; вычистить TODO. **Вычитка владельцем ДО подключения** (особо — раздел цен).
- [ ] **Режим «Задать вопрос»**: Haiku + kb в системнике. Жёсткие правила: цены только цитатой из kb, при неуверенности — эскалация в заявку, троттлинг на юзера.
- [ ] **«Взаимный сторож»**: Vercel-крон пингует Railway /health → алерт в наш ТГ (бесплатная замена платному UptimeRobot ТГ).

### Инфра

- [ ] **Pre-push hook в репо**: `node --check video-server/index.js` сейчас только в `.git/hooks` (умрёт при переклонировании). Задача: перенести в `scripts/` + настроить `core.hooksPath` + покрыть все .js файлы.
- [ ] **Миграция `video_jobs.warnings jsonb`**: `ALTER TABLE public.video_jobs ADD COLUMN IF NOT EXISTS warnings jsonb;` — нужна для полноценной деградации libass. Проверить выполнена ли.

### Мелочи

- [ ] Шапка сайта: «—» вместо баланса (воспроизводится у владельца, повторяется из дельты №3).
- [ ] Живой тест рассылок/сгорания на реальном наступлении срока (напоминание и уведомление о сгорании проверены только моками).
- [ ] Слабые «зомби»: `image_style`, `voice_id` pre-select из прошлой сессии видны до платного клика — низкий приоритет.
- [ ] Живой тест сценария (б) фазы-зомби: пропуск Шага 5 → Phase 5 skipped — так и не прогнан владельцем.

---

## БЭКЛОГ (зафиксированный, без строгого приоритета)

- **Референс-изображение** — итерация 2 иллюстраций (приоритет 1 после запуска платежей).
- **Свести платёжные входы** (в пакете запуска): лендинг «Выбрать тариф» + ЛК «Оплатить из России» → `/billing`.
- **TOOL_SLUG_REMAP** — пластырь: БД хранит `image-illustrations`, маршрут `/tools/illustrations`. Хранить слаг в одном виде, убрать таблицу переводов.
- **Атомарность claim** `bot_settings`: SELECT→INSERT (теоретическая гонка при одновременной доставке). Правильно — уникальный индекс на `key`, чтобы дубль отбивала БД.
- Юрист на вычитку оферты/соглашения **перед масштабированием**.
- Тайминг Phase 5 на длинных видео: worst-case с ретраем до 60 мин. При жалобах — снизить per-attempt таймаут второй попытки (транзиенты быстрые: DECISIVE 4.2 мин).
- Paddle `subscription.updated` (смена тарифа mid-cycle): кредиты не начисляются до следующего `transaction.completed` — осознанно, но пересмотреть при живых Paddle-юзерах.
- Resend/email-канал: отложено (ТГ-only достаточно; триггер — живой юзер без ТГ).
- B2 egress-порог в панель балансов — приоритет упал после R2-миграции.

---

## ЗАКРЫТО (сессии 17–22.07.2026)

- ✅ R2-миграция финальных видео + CF cache rule + опровержение WAF-вердикта (SHA 1cf33b2).
- ✅ startup-recovery / watchdog (DRY_RUN=false) / SIGTERM / heartbeat — video-server (SHA ea7b622).
- ✅ Railway Watch Paths = `video-server/**` (SKIPPED при безопасных пушах).
- ✅ Тайм-бюджет генерации сцен + фантом «8192» + fallbackTopic + sanitizeScenePrompt.
- ✅ Фаза-зомби субтитров — `subtitlesConfirmedThisSession` (SHA 2028520).
- ✅ libass Phase 5: retry (2 попытки, 10с), heartbeat, честная деградация — янтарный баннер (SHA 7c223cb + 0886d63).
- ✅ Инцидент цены ×100 в ТГ-боте (PAY_PLANS) → `/api/plans` единый источник (SHA 3a5684a + 562341a).
- ✅ PROD DOWN 4ч24м (APP_URL дубль) → pre-push hook локально, UptimeRobot (SHA 8e87f8a).
- ✅ Миграции 001–005 в проде: два кошелька, expire, retention, legal_acceptances, type-колонка.
- ✅ Подписочная модель v1: два кошелька, expire_plan cron, рассылки, extend_plan, billing-UI.
- ✅ Волна 1 инструментов (7 штук).
- ✅ Retention 72ч: MediaBadge, крон, env унификация, DRY_RUN=false (SHA 5c969a8+).
- ✅ 529 overload fix: maxRetries:0 + withAnthropicRetry 16±4с (SHA в anthropic-retry.ts).
- ✅ content[0] × 19 мест → `.find(b => b.type === 'text')` (SHA e0de73a).
- ✅ ЮKassa: create + webhook + living test (plan + topup + idempotency + incidents).
- ✅ Юрдокументы (offer/terms/privacy/refund) + legal_acceptances + миграция 006+007.
- ✅ Витрина под модерацию (Google OAuth флаг, чекбокс согласия, рублёвый прайс, «перевод по карте» убран).
- ✅ NSFW-фильтр fal: 6 генераторов, авторетрай, янтарный щит (SHA cfaf65b).
- ✅ Волна 2 — Субтитры по аудио (SHA 036b3a3 + 442b63f + 40803f2).
- ✅ Волна 3 — Иллюстрации по тексту (SHA b451ec7 + 625f37b).
- ✅ ЮKassa webhook: body.event вместо body.type (SHA 4587d78).
- ✅ IP whitelist ЮKassa + verbose logging (SHA 01e17cb).
- ✅ payment_incidents + recordIncident во всех failure-ветках (SHA 6a38589). Миграция 008 в проде.
- ✅ Billing: понижение тарифа разрешено + кнопка «Продлить» (SHA 9989791).
- ✅ Тест идемпотентности (test endpoint удалён — SHA b7bdae9, 9901805).
